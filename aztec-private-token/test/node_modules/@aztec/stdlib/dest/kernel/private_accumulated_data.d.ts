import { MAX_CONTRACT_CLASS_LOGS_PER_TX, MAX_ENQUEUED_CALLS_PER_TX, MAX_L2_TO_L1_MSGS_PER_TX, MAX_NOTE_HASHES_PER_TX, MAX_NULLIFIERS_PER_TX, MAX_PRIVATE_CALL_STACK_LENGTH_PER_TX, MAX_PRIVATE_LOGS_PER_TX } from '@aztec/constants';
import { BufferReader } from '@aztec/foundation/serialize';
import { ScopedCountedL2ToL1Message } from '../messaging/l2_to_l1_message.js';
import { ClaimedLengthArray } from './claimed_length_array.js';
import { ScopedCountedLogHash } from './log_hash.js';
import { ScopedNoteHash } from './note_hash.js';
import { ScopedNullifier } from './nullifier.js';
import { PrivateCallRequest } from './private_call_request.js';
import { ScopedPrivateLogData } from './private_log_data.js';
import { CountedPublicCallRequest } from './public_call_request.js';
/**
 * Specific accumulated data structure for the final ordering private kernel circuit. It is included
 *  in the final public inputs of private kernel circuit.
 */
export declare class PrivateAccumulatedData {
    /**
     * The new note hashes made in this transaction.
     */
    noteHashes: ClaimedLengthArray<ScopedNoteHash, typeof MAX_NOTE_HASHES_PER_TX>;
    /**
     * The new nullifiers made in this transaction.
     */
    nullifiers: ClaimedLengthArray<ScopedNullifier, typeof MAX_NULLIFIERS_PER_TX>;
    /**
     * All the new L2 to L1 messages created in this transaction.
     */
    l2ToL1Msgs: ClaimedLengthArray<ScopedCountedL2ToL1Message, typeof MAX_L2_TO_L1_MSGS_PER_TX>;
    /**
     * Accumulated logs from all the previous kernel iterations.
     */
    privateLogs: ClaimedLengthArray<ScopedPrivateLogData, typeof MAX_PRIVATE_LOGS_PER_TX>;
    /**
     * Accumulated contract class logs from all the previous kernel iterations.
     * Note: Truncated to 31 bytes to fit in Fr.
     */
    contractClassLogsHashes: ClaimedLengthArray<ScopedCountedLogHash, typeof MAX_CONTRACT_CLASS_LOGS_PER_TX>;
    /**
     * Accumulated public call requests from all the previous kernel iterations.
     */
    publicCallRequests: ClaimedLengthArray<CountedPublicCallRequest, typeof MAX_ENQUEUED_CALLS_PER_TX>;
    /**
     * Current private call stack.
     */
    privateCallStack: ClaimedLengthArray<PrivateCallRequest, typeof MAX_PRIVATE_CALL_STACK_LENGTH_PER_TX>;
    constructor(
    /**
     * The new note hashes made in this transaction.
     */
    noteHashes: ClaimedLengthArray<ScopedNoteHash, typeof MAX_NOTE_HASHES_PER_TX>, 
    /**
     * The new nullifiers made in this transaction.
     */
    nullifiers: ClaimedLengthArray<ScopedNullifier, typeof MAX_NULLIFIERS_PER_TX>, 
    /**
     * All the new L2 to L1 messages created in this transaction.
     */
    l2ToL1Msgs: ClaimedLengthArray<ScopedCountedL2ToL1Message, typeof MAX_L2_TO_L1_MSGS_PER_TX>, 
    /**
     * Accumulated logs from all the previous kernel iterations.
     */
    privateLogs: ClaimedLengthArray<ScopedPrivateLogData, typeof MAX_PRIVATE_LOGS_PER_TX>, 
    /**
     * Accumulated contract class logs from all the previous kernel iterations.
     * Note: Truncated to 31 bytes to fit in Fr.
     */
    contractClassLogsHashes: ClaimedLengthArray<ScopedCountedLogHash, typeof MAX_CONTRACT_CLASS_LOGS_PER_TX>, 
    /**
     * Accumulated public call requests from all the previous kernel iterations.
     */
    publicCallRequests: ClaimedLengthArray<CountedPublicCallRequest, typeof MAX_ENQUEUED_CALLS_PER_TX>, 
    /**
     * Current private call stack.
     */
    privateCallStack: ClaimedLengthArray<PrivateCallRequest, typeof MAX_PRIVATE_CALL_STACK_LENGTH_PER_TX>);
    toBuffer(): Buffer<ArrayBufferLike>;
    toString(): `0x${string}`;
    /**
     * Deserializes from a buffer or reader, corresponding to a write in cpp.
     * @param buffer - Buffer or reader to read from.
     * @returns Deserialized object.
     */
    static fromBuffer(buffer: Buffer | BufferReader): PrivateAccumulatedData;
    /**
     * Deserializes from a string, corresponding to a write in cpp.
     * @param str - String to read from.
     * @returns Deserialized object.
     */
    static fromString(str: string): PrivateAccumulatedData;
    static empty(): PrivateAccumulatedData;
}
//# sourceMappingURL=private_accumulated_data.d.ts.map