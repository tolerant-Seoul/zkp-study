import { z } from 'zod';
/**
 * Defines the possible states of the world state synchronizer.
 */ export var WorldStateRunningState = /*#__PURE__*/ function(WorldStateRunningState) {
    WorldStateRunningState[WorldStateRunningState["IDLE"] = 0] = "IDLE";
    WorldStateRunningState[WorldStateRunningState["SYNCHING"] = 1] = "SYNCHING";
    WorldStateRunningState[WorldStateRunningState["RUNNING"] = 2] = "RUNNING";
    WorldStateRunningState[WorldStateRunningState["STOPPED"] = 3] = "STOPPED";
    return WorldStateRunningState;
}({});
export const WorldStateSyncStatusSchema = z.object({
    finalizedBlockNumber: z.number().int().nonnegative(),
    latestBlockNumber: z.number().int().nonnegative(),
    latestBlockHash: z.string(),
    oldestHistoricBlockNumber: z.number().int().nonnegative(),
    treesAreSynched: z.boolean()
});
